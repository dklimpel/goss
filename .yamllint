---
extends: default

ignore:
  # uses go templates (invalid yaml)
  - integration-tests/goss/goss-shared.yaml
  - docs/goss.yaml

rules:
  braces:
    min-spaces-inside: 0
    max-spaces-inside: 1
  indentation:
    spaces: consistent
    indent-sequences: consistent
  line-length: disable
  document-start: disable
  truthy:
    allowed-values:
      - "on"
      - "false"
      - "true"
